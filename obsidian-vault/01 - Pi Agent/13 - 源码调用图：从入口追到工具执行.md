---
title: "源码调用图：从入口追到工具执行"
module: "Pi Agent"
source_html: "modules/pi-agent/callgraph.html"
site_url: "https://cuizihao1992.github.io/learn-pi/modules/pi-agent/callgraph.html"
tags:
  - learn-pi
  - module/pi-agent
---

> Reference

# 源码调用图：从入口追到工具执行

这一页给你“读代码路线”。不知道从哪里跳到哪里时，就按这些图走。

## 主路径：一次用户输入

```text
packages/coding-agent/src/main.ts
  -> createAgentSessionRuntime()
  -> createAgentSessionServices()
  -> createAgentSessionFromServices()
  -> runInteractiveMode() / runPrintMode() / runRpcMode()
  -> AgentSession.prompt()
  -> Agent.run()
  -> runAgentLoop()
  -> runLoop()
  -> streamAssistantResponse()
  -> pi-ai streamSimple()
```

## 工具路径：模型返回 toolCall 后

```text
runLoop()
  -> message.content.filter(type === "toolCall")
  -> executeToolCalls()
  -> executeToolCallsParallel() or executeToolCallsSequential()
  -> prepareToolCall()
  -> validateToolArguments()
  -> executePreparedToolCall()
  -> finalizeExecutedToolCall()
  -> createToolResultMessage()
  -> currentContext.messages.push(toolResult)
```

## 工具注册路径

```text
AgentSession._buildRuntime()
  -> createAllToolDefinitions()
  -> createToolDefinition(read/bash/edit/write/grep/find/ls)
  -> wrapRegisteredTools()
  -> _toolDefinitions / _toolRegistry
  -> Agent.state.tools
```

## 前端事件路径

```text
runLoop emit AgentEvent
  -> Agent.subscribe(listener)
  -> AgentSession._handleAgentEvent()
  -> AgentSessionEvent
  -> interactive-mode listener
  -> components update
  -> pi-tui render
```

## 压缩路径

```text
AgentSession._handlePostAgentRun()
  -> estimateContextTokens()
  -> shouldCompact()
  -> prepareCompaction()
  -> generateSummary()
  -> compact()
  -> session entries include summary message
```

## 参考答案：练习 1 调用图

如果你做了 labs 里的“画出一次 prompt 调用图”，参考答案可以是：

```text
用户输入
  -> interactive-mode 捕获输入
  -> AgentSession.prompt(text)
  -> create user AgentMessage
  -> Agent.run([message])
  -> runAgentLoop()
  -> runLoop()
  -> streamAssistantResponse()
  -> pi-ai provider stream
  -> assistant message with toolCall
  -> executeToolCalls()
  -> toolResult
  -> runLoop next turn
  -> assistant final text
  -> agent_end
```

深挖工具路径
[[01 - Pi Agent/05 - Pi Agent：toolCall 如何变成 toolResult|把 toolCall 到 toolResult 讲透]]
